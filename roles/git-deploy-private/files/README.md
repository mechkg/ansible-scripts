This directory should contain the private key that gives access to the private GitHub repository
for the 'deploy' user on all managed hosts.

The SSH private key should be in a file named 'github-deploy'
